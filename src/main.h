#ifndef _GRAPROJ_MAIN_HEADER_
#define _GRAPROJ_MAIN_HEADER_

// This function keeps initialization code for the system (SDL for now)
// Returns 1 if everything is ok, 0 otherwise
int init_system(void);

// Deinitialization function for the system
void deinit_system(void);

#endif /* _GRAPROJ_MAIN_HEADER_ */
